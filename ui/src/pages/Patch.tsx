export function Patch() {
  return (
    <div className="space-y-2">
      <h1 className="text-2xl font-semibold tracking-tight">Patch & Personalities</h1>
      <p className="text-muted-foreground">
        Define fixture personalities (per-channel fade / snap behaviour) and patch them to DMX
        addresses. Coming next — this is what teaches the engine which channels snap instead of fade.
      </p>
    </div>
  );
}
